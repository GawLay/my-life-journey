# World atlas data

`world-countries.geojson` is the 1:50m Admin 0 Countries dataset from
[Natural Earth](https://www.naturalearthdata.com/). Natural Earth data is in
the public domain. The local copy keeps the interactive globe self-contained
and available without a runtime network request. Nonessential feature
properties were removed; the geographic coordinates are unchanged.
